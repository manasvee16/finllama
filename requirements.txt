# FINLLAMA — Python dependencies
# Install PyTorch with the correct CUDA build from https://pytorch.org/get-started/locally/ if using GPU.
# Example (CUDA 12.x): pip install torch --index-url https://download.pytorch.org/whl/cu124

# Core ML / Hugging Face
torch>=2.1.0
transformers>=4.36.0
datasets>=2.16.0
accelerate>=0.25.0
peft>=0.7.0
safetensors>=0.4.0
huggingface_hub>=0.20.0

# Data I/O & numerics (Parquet via pandas/datasets needs pyarrow)
numpy>=1.24.0
pandas>=2.0.0
pyarrow>=14.0.0
scikit-learn>=1.3.0
tqdm>=4.66.0

# Configuration
python-dotenv>=1.0.0

# Portfolio / market data
yfinance>=0.2.30

# Optional: plots in utils/evaluate_all.py (confusion matrices)
matplotlib>=3.8.0
seaborn>=0.13.0
